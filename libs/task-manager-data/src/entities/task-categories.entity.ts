export enum TaskStatus {
    NOT_STARTED = 1,
    IN_PROGRESS = 2,
    AWAITING_RESPONSE = 3,
    AWAITING_MEETING = 4,   
    COMPLETED = 5
}

export enum IntervalUnit {
    MINUTES = 1,
    HOURS = 2,
    DAYS = 3,
    MONTHS = 4
}